apiVersion: v1
kind: Service
metadata:
  name: {{ include "novacms.fullname" . }}-ollama
  labels:
    {{- include "novacms.labels" . | nindent 4 }}
    app.kubernetes.io/component: ollama
spec:
  type: ClusterIP
  ports:
    - port: 11434
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "novacms.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ollama
